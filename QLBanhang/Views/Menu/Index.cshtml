@using QLBanhang.Models;
<ul class="dropdown-menu">
    @foreach (var item in ViewBag.TenLoaiSP)
    {
        <li class="dropdown-menu-item">
            <a href="@Url.Action("Index", "Home", new { MaLoaiSP = item.Key })">@item.Value</a>
        </li>
    }
</ul>

